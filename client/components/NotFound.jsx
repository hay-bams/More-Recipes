import React from 'react';
import Navigation from './Navigation';

const NotFound = () => (
  <div className="container text-center" id="recipes">
    <h1 style={{ color: '#000' }}> Are You Lost ? </h1>
    <h5> This page does not exist (404)</h5>
  </div>
);

export default NotFound;
